export class Character {
  rank: number
  name: string
  url: string
  kakera: number

  constructor (rank: number, name: string, url: string, kakera: number) {
    this.rank = rank
    this.name = name
    this.url = url
    this.kakera = kakera
  }
}
